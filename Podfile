# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PotFinder' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PotFinder
  pod 'Firebase'
  pod 'Firebase/Auth'
  pod 'RxSwift'
  pod 'Alamofire'
  pod 'ObjectMapper'
  pod 'Cloudinary'

  target 'PotFinderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PotFinderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
